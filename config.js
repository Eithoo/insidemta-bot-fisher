const config = {
	activationKey: 'F7', // klawisz do włączania i wyłączania bota
	fishingRodType: 0, // rodzaj wędki - do wyboru: 0 (kazda oprocz +9), 1 (+9)
	resolution: 'FHD' // wybrana rozdzielczosc - obecnie dostepna tylko FHD, jak ktos potrzebuje innej to wyslac mi nagranie 30 sekundowe w dobrej jakosci z łowienia na wybranej rodzielczosci, to wrzuce do kodu wymiary
}

module.exports = config;